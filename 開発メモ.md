# 開発メモ（天井伏図生成）

作業を中断した時点の記録です。再開するときはここから読んでください。

最終更新: 2026-08-15（寸法線対応とレイヤ並べ替えまで完了） / ブランチ `claude/pdf-loading-pbljw9`

## Jw_cad で使うだけなら（git は不要）

`jww_ceiling_plan.zip` を展開してできる「天井伏図生成」フォルダを、Jw_cad の外部変形フォルダ（例 `C:\jww\外変\`）にコピーするだけです。GitHub からは次の場所で入手できます。

- ZIP 直リンク … `https://github.com/Scarab-11/s-claude-2/raw/claude/pdf-loading-pbljw9/jww_ceiling_plan.zip`
- ブランチのページ … `https://github.com/Scarab-11/s-claude-2/tree/claude/pdf-loading-pbljw9`

以下は、コードを直す側の話です。

## いまどこまでできているか

`dist/` にそのまま使える状態で入っています（`jww_ceiling_plan.zip` はこの一部をまとめたもの）。Jw_cad の外部変形フォルダにコピーすれば動きます。

- 平面図を範囲選択 → 作図位置を1点指示 → 天井伏図のたたき台を作図
- 元の平面図は消えない（出力に `hd` を書かないため）
- レイヤの取捨、室名→天井仕上記号（四角枠つき）、面積表記の削除、図面名の書き換え、仕上表の作図
- **記号と仕上表は全自動**。`ROOM_LAYER` のレイヤの文字は全部が室名。記号は ①`ROOM` の登録 → ②図面の内部仕上概要表から読んだ天井仕上が同じ室と同じ記号 → ③新しい番号、の順で決まる。仕上表もその結果から作る。室名を事前登録する方式では実務に耐えないという指摘を受けての設計変更
- **仕上表の読み取り (`finish_table`)** は罫線を見ない。室名レイヤ以外に室名と同じ文字が3つ以上並ぶレイヤを表とみなす。2枚目の図面で壊れた出力になったのは、ここの前提が弱かったため:
  - 右の欄かどうかを**文字の末尾**で見ていた → 長い室名が欄からはみ出すと右の欄を取りこぼす。**書き出し位置**で見るように変更
  - 行の帯を「最小の行間 × 0.45」の決め打ちにしていた → 2行書きの行と1行の行が混ざる表で外れる。**隣の行との中間で区切る**方式に変更（`row_bounds`）。上端・下端も1行分で打ち切らないと、見出しや表題が最初の行に吸い込まれる
  - 「収納1・収納2」のように1セルに2室書いてある → 中黒などで切った分も照合する
  - 仕上の突き合わせは**空白を取ってから**。「木軸下地　石膏…」と「木軸下地石膏…」が別記号になっていた
  - 表と図面で枝番が食い違う（表が「倉庫」、図面が「倉庫①」）ので、頭一致は双方向に見る
  - 仕上が読めなかった記号の凡例は空欄にせず室名を入れる。空行が並ぶと表が壊れて見える
  - 3枚目の図面（S=1/200・概要表が2つ）で「仕上の欄に室名が混ざる」として出た件:
    右の欄の境目を室名セルの x の**最小**で決めていた → 欄の中で中央寄せされた室名が、自分自身の仕上として拾われる。**最大**で決めるように変更
    行の目印を「図面の室名と一致したセル」だけにしていた → 表にあって図面に無い室（裏玄関など）の行が目印にならず、その行の文字が隣の行の仕上に流れ込む。**室名の欄にある文字を全部**目印にするよう変更
- `NOROOM` で UP/DN/PS を除外。`ROOM_AUTO off` で従来どおり登録した室名だけにもできる
- **壁の中心線から W120 の壁線を生成**（`make_walls`）。壁を「中心線を軸とする長方形」の集まりとみて、他の長方形の内側に入った辺を落とす＝和集合の外周を描く。取り付く端は半幅だけ伸ばして角を塞ぐ。最後に同一直線上の切れ端をつなぐ。shapely 不要
- **寸法線も写せる**（`KEEP` に足すだけ）。寸法値 `cs`・点の属性 `pn` に対応済み。`COLOR` は種類（text/line/point/arc）別に指定できる
- **`KEEP 0:8 => 0:3`** で天伏図側のレイヤを並べ直せる。記号のレイヤは埋まっている番号を自動で避ける
- 記号は `SYMBOL_LAYER`、仕上表は `LEGEND_LAYER` と別レイヤに分けて書き込む
- 仕上表の線色は外枠(`LEGEND_BORDER_COLOR`)と罫線(`LEGEND_LINE_COLOR`)で分ける
- 実装は Python（`src/jww_ceiling_plan.py`）。Ruby 版は移植後に削除（履歴の `46f6289` に残存）

### 動作確認の状況

実機で 2 件出た不具合（`#h0` で範囲選択に入らない／縮尺の換算漏れ）は修正済み。それ以外は `jwc_temp.txt` を模した CP932 のサンプルを通した範囲でのみ確認しています。サンプルは実寸（1/100・7.28m×8.19m）で書いてあります。

```sh
# テストの回し方（Linux/macOS。Windows なら dist/テスト/天伏図テスト実行.bat）
python3 -c "import io; open('/tmp/jwc_temp.txt','wb').write(
  io.open('src/テスト/sample_平面図.txt',encoding='utf-8').read().replace('\n','\r\n').encode('cp932'))"
python3 -c "import io; open('/tmp/rule.txt','wb').write(
  io.open('src/天伏図ルール.txt',encoding='utf-8').read().replace('\n','\r\n').encode('cp932'))"
python3 src/jww_ceiling_plan.py /tmp/jwc_temp.txt /tmp/rule.txt
iconv -f CP932 -t UTF-8 /tmp/jwc_temp.txt
```

**`src/` を直したら、必ず `python3 tools/test.py` を通してから配ること。** 過去に出た不具合を 1 件ずつ確かめる回帰テスト。実機で見つかった不具合を直したら、必ず 1 件足す。その場かぎりの確認しかしていなかったせいで、直したところが次の変更で壊れる、を繰り返した。

```sh
python3 tools/test.py          # 全部
python3 tools/test.py 壁線     # 名前で絞る
```

`src/` を直したら `python3 tools/build.py` で `dist/` を作り直します。`.py` は CP932 に変換されません（Python 3 のソースは UTF-8 で読まれるため）。配布用 ZIP を作り直すときは `python3 tools/make_zip.py`。

## 決まっていること

| 項目 | 決定 |
|---|---|
| 実装言語 | Python 3.8+（利用環境は 3.11） |
| 天井仕上の文字 | **(b) 空レイヤを用意して、そこへ記号を書き込ませる** |
| 平面図のレイヤ | 壁の中心線（一点鎖線）/ 部屋名 / 基準線 / 天井仕上の文字 に分けられる |
| スコープ | まず最小構成。閉領域抽出はフェーズ2 |

## 解決済み: 3枚目の図面「多目的室」の行が読めない件（8/15）

`jwc_temp_控え.txt` を受け取り、原因が特定できた。

### 原因

概要表の「多目的室」という文字が、**Jw_cad の文字の中で Enter を押して
2行にした形**で書かれていた（1行目が空文字、2行目に本文がそのまま続く）。
jwc_temp.txt の書式では、2行目以降は `ch x y f3 f4 "` のような見出しが
付かず、文字列だけがそのまま次の行に来る。うちのパーサはこの継続行を
知らず、素の1行として現れた「多目的室」を「対応していない要素」に
分類していた（ログの `多 … 1 個   例: 多目的室` はこれ）。
見出しの `ch` 行自体は空文字として読めていたので、行そのものは
（`表からはこう読めています` に出ない形で）静かに落ちていた。

他の室で同じ問題が起きなかったのは、たまたま Enter が入っていなかった
から。「US」の仕上が読めなかった件は別の理由（そもそも概要表にその
室の行が無い＝実データ側の欠落）で、これとは無関係。

### 直した内容

`Reader.parse` に `continue_text()` を追加（`src/jww_ceiling_plan.py`）。
直前に読んだ要素が文字で、今の行が英字始まりでない（＝ `ch` のような
知っている命令名ではない）ときだけ、前の文字の続きとみなしてつなげる。
`msg`/`cv` のような命令はすべて英字始まりなので、これらを継続行と
誤認することはない（`cs`/`pn` はこのあと対応した。下の「寸法線まわり」）。

回帰テスト「概要表の室名が2行の文字でも読める」を追加。修正前のコードで
実際に NG になる（＝実機の症状を再現できる）ことを確認済み。

過去2回の試み（b79eda5 の行高さ上限、a80d669 の改行の切り方変更）は
どちらも無関係ではなく安全側の改善として残しているが、今回の本当の
原因はどちらでもなかった。特に a80d669 の「制御文字で行が分断される」
説は誤りで、実際には正規の複数行文字だった。

### 積み残し（急ぎではない）

`cv`(5) `cn`(3) はまだ追っていない。除外レイヤ側に出ており、出力には
影響していない。`cn"$<ＭＳ ゴシック>` はフォント指定付きの `cn` で、
`ATTR_LINE` の `cn[0-9a-fA-F]` に当たらないための取りこぼし。
次に読み取り漏れが疑われたら `skipped_at` 経由で調べること。

## 解決済み: 寸法線まわり（8/15）

「天伏図にも寸法線を出したい」から始まって、3 つ別々の原因が出た。
どれも**属性行・要素行を 1 つ取りこぼすと、写した先で見た目が変わる**
という同じ形をしている。ログの「対応していない要素」は軽視しないこと。

| 症状 | 原因 | 直し方 |
|---|---|---|
| 数字が出ない | 寸法値は `ch` ではなく **`cs`**（形は `ch` と同じ）。`msg` はその直前に付く中身の無い印 | `cs` を `ch` と同じ文字要素として読む。`msg` は読み捨て |
| 実点の色が変わらない | **`pn`**（点の種類＝実点/仮点と大きさ）を読み捨てていた。属性なので点89個に対し `pn6` が1行だけ出る | `lg`/`ly`/`lc`/`lt`/`cn` と同じ属性として扱い、書き出しにも含める |
| 線まで色が変わる | `COLOR` がレイヤ単位でしか指定できなかった | `COLOR 0:8 6 text` のように **種類**（text/line/point/arc）を付けられるようにした |

診断のために入れたもの（次に似たことが起きたら、まずここを見る）:

- 「対応していない要素」に**必ず中身のサンプルを出す**。以前は英字だけの
  キー（`cs`/`msg`）だとサンプルを省いており、これが原因究明を 2 往復
  遅らせた。省略の条件を外した
- レイヤ別内訳で**線・点・円を分けて数える**（実点が点なのか円なのか
  長さ0の線なのか、これが分からないと切り分けられない）
- ログに「線色を変更」の件数。COLOR が効いているかどうかの切り分け用。
  これが 150（数字61＋点89）と出たことで、こちら側は正しく色を書いて
  いる＝点の**種類**が違う、と分かった

## 解決済み: 天伏図側でレイヤを並べ直す（8/15）

記号のレイヤが寸法線とぶつかった。記号の数は計画によって増減するので、
起点をずらすだけでは防げない。2 段構えにした。

1. **`KEEP 0:8 => 0:3`** で写す先のレイヤを変えられるようにした
   （`Rules.relayer`、`CeilingPlan.relayer`）。並べ替えは**最後**に行うので、
   `WALL_FROM` や `ROOM_LAYER` などの他の設定は元の図面の番号のまま書ける
2. 記号のレイヤは**並べ替えたあとの番号**で空きを探す（`occupied_layers`）。
   KEEP したレイヤと `LEGEND_LAYER` を自動で飛ばし、飛ばした番号はログに
   出す。1 グループ(16枚)で足りなければ次のレイヤグループへ続ける

既定の並び:

```
0:1 基準線 / 0:2 部屋名 / 0:3 寸法線 / 0:4 仕上表・図面名
0:5 壁線・壁の中心線 / 0:6〜 記号
```

`SYMBOL_LAYERS` で手で並べた指定は、意図した配置を変えないようそのまま使う。

## 配布について

`.py` なので**コピー先にも Python 3.8 以上が要る**。標準ライブラリだけで
書いてあり、3.10／3.11／3.12／3.13 で同じ結果になることを確認済み
（`import` は datetime/math/os/re/sys/unicodedata のみ）。
walrus 演算子を使っているので 3.8 未満は不可。`match`/`case` や
`removeprefix` のような 3.9+ 専用の書き方は使っていない。

Python が無い環境では `.bat` が `he …` を書いて Jw_cad にメッセージを
出す（図面は壊れない）。exe 化（PyInstaller）は 30MB 前後になり誤検知の
恐れもあるので、いまの形のまま配る方針。

### 実機で確認済みのレイヤ構成（3枚目の図面）

```
0:1 基準線・通り芯（線種1が102, 5が61）   0:2 部屋名   0:3 建具
0:4 ?   0:5 設備ほか   0:6 内部仕上概要表   0:7 ?   0:8 家具・文化財ライン
0:9 ?   0:a 図面名(（３）平面図)   0:c ?   0:d 図面名   0:f 壁の中心線（線種5）
```

## 次にやること（フェーズ2の案）

壁の中心線が独立レイヤにあるので、`shapely` で閉領域を起こせます。効果が見えやすい順に:

1. **壁芯線から閉領域を作る** — `shapely.ops.polygonize(壁芯線の集合)` で各室のポリゴンが出る。芯線なので建具開口で切れる心配がない。`make_walls` で長方形の和集合は自前で解いているので、閉領域も同じ考えで書けなくはない。
2. **部屋名の文字座標がどのポリゴンに入るかを内包判定** → 領域と室名の対応を取る。
3. **記号を部屋の重心に置く**（いまは室名の真下）。ここまでで一区切りにするとよい。
4. 外周リングを `buffer(軒の出)` して**軒天ライン**を生成。
5. ポリゴン面積から**天井面積**を拾い、仕上表に面積欄を足す。
6. 領域をクリップ範囲にして**目透かし目地**を割り付ける。

`pip install shapely` が要ります。単体 exe 化（PyInstaller）する場合、shapely 込みで 30MB 前後になります。

## 設計のメモ（読み書きの要点）

`jwc_temp.txt` は CP932 / CRLF。**座標は実寸 mm**。1/100 の図面で `7280` とあれば 7.28m。`hs` は縮尺の分母がレイヤグループ 0-15 分だけ並ぶ。

一方 **`hcw`/`hch`/`hcd`（文字の幅・高さ・間隔）は図寸 mm**。ここが混ざるのが最大の落とし穴で、実機で最初に踏んだ。`SYMBOL_DY` などの設定値も図寸 mm なので、座標に使うときは必ず縮尺の分母を掛ける（`CeilingPlan.scale(lg)`）。掛け忘れると 1/100 の図面で長さが 1/100 になり、記号も仕上表も一点に潰れて重なる。`SET SCALE` で固定もできる。

出力側で `bz` を書くと座標を図寸として解釈させられるが、複写する元要素は実寸で受け取っているので使っていない。

```
hcw / hch / hcd   文字種1-10 の 幅 / 高さ / 間隔
hs 100            縮尺 1/100
hp1 x y           REM #1 で指示した点
lg0 ly0 lc2 lt1 cn3   属性。以降の要素に効く
pn6               点の種類（実点/仮点と大きさ）。これも効きっぱなし
x1 y1 x2 y2       線
ci cx cy r [開始角 終了角 …]   円・円弧
ch x y f3 f4 "文字列           文字（閉じquoteなし）
cs x y f3 f4 "文字列           寸法値。形は ch と同じ
msg                            cs の直前に付く印。中身は無い
```

引っかかりやすい点:

- **レイヤ a〜f（16進）**。属性行 `lyf` の属性名を `[a-z]+` で取ると `lyf` 全体が名前になり、レイヤ番号が更新されないまま次の要素へ持ち越される。実機で「レイヤFの壁が丸ごと消える」「建具が別レイヤ扱いで残る」として現れた。属性名は先頭2文字で取る。`src/テスト/sample_16進レイヤ.txt` が回帰用。
- **属性行の見分け**は `lg/ly/lc/lt/cn` のホワイトリストで行う。`[a-z]` 始まりを全部属性扱いにすると、ソリッド `sl` やブロック `bl` が属性として取り込まれ、書き出しにそのまま出てしまう。
- **行の切り方は `\r\n|\r|\n` だけ**。`str.splitlines()` は縦タブ(0x0B)・改頁(0x0C)・ファイル分離符(0x1C-0x1E)・NEL(0x85) なども改行として切る。文字の中にそういう制御文字が1つ混ざっているだけで、その要素が「`ch x y … "`」と「本文」に分断され、行ごと読めなくなる。実機で概要表の「多目的室」の行だけが落ち、その行の仕上が上下の行に流れ込んだ原因がこれ。
- **`ch` の第3・第4フィールド**は「長さ成分」と「終点座標」の2説がある。読み込んだデータから多数決で自動判定している（`Doc.detect_ch_format`）。`SET CHFORMAT rel|abs` で固定可。
- **文字列を書き換えたら長さも取り直す**こと。しないと Jw_cad 側で文字が引き伸ばされる。
- **文字幅の半角判定**は Shift_JIS で1バイトになる文字（ASCII と**半角カナ**）。`ｹｲ酸ｶﾙｼｳﾑ板` のような表記が仕上表に入るので、ここを間違えると欄幅がずれる（移植時に実際に踏んだ）。
- **扱う要素は線・点・円弧・文字だけ**。ソリッドやブロック図形は複写されず、ログに個数が出る。
- ルールファイルは、行頭（桁下げなし）が英大文字だけの語で始まる行だけを項目として読む。それ以外は説明文扱いなので `#` を付けなくてよい。

## 元になった資料

`f07791c0`（1階平面図 S=1/100）と `9137cb8f`（1階天井伏図 S=1/100）の2枚。事務所併用住宅。仕上表の C-1〜C-5 と室名→記号の対応は、この2枚から `src/天伏図ルール.txt` に写してあります。
