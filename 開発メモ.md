# 開発メモ（天井伏図生成）

作業を中断した時点の記録です。再開するときはここから読んでください。

最終更新: 2026-08-11 / ブランチ `claude/pdf-loading-pbljw9`

## Jw_cad で使うだけなら（git は不要）

`jww_ceiling_plan.zip` を展開してできる「天井伏図生成」フォルダを、Jw_cad の外部変形フォルダ（例 `C:\jww\外変\`）にコピーするだけです。GitHub からは次の場所で入手できます。

- ZIP 直リンク … `https://github.com/Scarab-11/s-claude-2/raw/claude/pdf-loading-pbljw9/jww_ceiling_plan.zip`
- ブランチのページ … `https://github.com/Scarab-11/s-claude-2/tree/claude/pdf-loading-pbljw9`

以下は、コードを直す側の話です。

## いまどこまでできているか

`dist/` にそのまま使える状態で入っています（`jww_ceiling_plan.zip` はこの一部をまとめたもの）。Jw_cad の外部変形フォルダにコピーすれば動きます。

- 平面図を範囲選択 → 作図位置を1点指示 → 天井伏図のたたき台を作図
- 元の平面図は消えない（出力に `hd` を書かないため）
- レイヤの取捨、室名→天井仕上記号（四角枠つき）、面積表記の削除、図面名の書き換え、仕上表の作図
- **`ROOM_LAYER` を指定すると、そのレイヤの文字は全部が室名**。`ROOM` に無いものには仮記号 `C-?` を置き、貼り付けられる `ROOM` 行をログに出す。室が増えるたびにルールファイルを直さなくても取りこぼさないため（実務で最初に詰まった点）。`NOROOM` で UP/DN/PS を除外
- **壁の中心線から W120 の壁線を生成**（`make_walls`）。壁を「中心線を軸とする長方形」の集まりとみて、他の長方形の内側に入った辺を落とす＝和集合の外周を描く。取り付く端は半幅だけ伸ばして角を塞ぐ。最後に同一直線上の切れ端をつなぐ。shapely 不要
- 記号は `SYMBOL_LAYER`、仕上表は `LEGEND_LAYER` と別レイヤに分けて書き込む
- 仕上表の線色は外枠(`LEGEND_BORDER_COLOR`)と罫線(`LEGEND_LINE_COLOR`)で分ける
- 実装は Python（`src/jww_ceiling_plan.py`）。Ruby 版は移植後に削除（履歴の `46f6289` に残存）

### 動作確認の状況

実機で 2 件出た不具合（`#h0` で範囲選択に入らない／縮尺の換算漏れ）は修正済み。それ以外は `jwc_temp.txt` を模した CP932 のサンプルを通した範囲でのみ確認しています。サンプルは実寸（1/100・7.28m×8.19m）で書いてあります。

```sh
# テストの回し方（Linux/macOS。Windows なら dist/テスト/天伏図テスト実行.bat）
python3 -c "import io; open('/tmp/jwc_temp.txt','wb').write(
  io.open('src/テスト/sample_平面図.txt',encoding='utf-8').read().replace('\n','\r\n').encode('cp932'))"
python3 -c "import io; open('/tmp/rule.txt','wb').write(
  io.open('src/天伏図ルール.txt',encoding='utf-8').read().replace('\n','\r\n').encode('cp932'))"
python3 src/jww_ceiling_plan.py /tmp/jwc_temp.txt /tmp/rule.txt
iconv -f CP932 -t UTF-8 /tmp/jwc_temp.txt
```

`src/` を直したら `python3 tools/build.py` で `dist/` を作り直します。`.py` は CP932 に変換されません（Python 3 のソースは UTF-8 で読まれるため）。配布用 ZIP を作り直すときは `python3 tools/make_zip.py`。

## 決まっていること

| 項目 | 決定 |
|---|---|
| 実装言語 | Python 3.8+（利用環境は 3.11） |
| 天井仕上の文字 | **(b) 空レイヤを用意して、そこへ記号を書き込ませる** |
| 平面図のレイヤ | 壁の中心線（一点鎖線）/ 部屋名 / 基準線 / 天井仕上の文字 に分けられる |
| スコープ | まず最小構成。閉領域抽出はフェーズ2 |

## 次に確認すること（実機が要る）

1. ~~`REM #h0` で図形データが渡ってくるか~~ → **実機で判明。`#h0` は「データ選択をしない」の意味で、範囲選択が始まらずいきなり `#1` の点指示に進んでしまう。`#h2`（範囲内＋内外にまたがる線）に変更済み。**
   参考: `#h1` 範囲選択して書き出す / `#h2` 内外にまたがる線も含める / `#h3` 文字のみ（文字位置揃えで使用）。`jwc_temp.txt` に `hd` を書くと `#h1`〜`#h4` で選択した図形が消える。
2. **`hd` を書かない＝元データが残る** の挙動。想定と違って元の平面図が消えるなら、作図位置の扱いを変える。
3. **実際のレイヤ番号**。`src/天伏図ルール.txt` の以下2箇所を実際の番号に直す。
   - `SET SYMBOL_LAYER 0:3` … 記号と枠の書き込み先（**仮の番号のまま**）
   - `SET LEGEND_LAYER 0:4` … 仕上表の書き込み先（**仮の番号のまま**）
   - `KEEP 0:0 / 0:1 / 0:2` … 残すレイヤ（壁線・基準線・部屋名）。**仮の番号のまま**

   番号が違っていても直せるように、ログの末尾にレイヤ別の内訳を出し、KEEP が空振りしたときのエラー文にも実際のレイヤ番号を並べている。失敗時もログは書く。

## 次にやること（フェーズ2の案）

壁の中心線が独立レイヤにあるので、`shapely` で閉領域を起こせます。効果が見えやすい順に:

1. **壁芯線から閉領域を作る** — `shapely.ops.polygonize(壁芯線の集合)` で各室のポリゴンが出る。芯線なので建具開口で切れる心配がない。`make_walls` で長方形の和集合は自前で解いているので、閉領域も同じ考えで書けなくはない。
2. **部屋名の文字座標がどのポリゴンに入るかを内包判定** → 領域と室名の対応を取る。
3. **記号を部屋の重心に置く**（いまは室名の真下）。ここまでで一区切りにするとよい。
4. 外周リングを `buffer(軒の出)` して**軒天ライン**を生成。
5. ポリゴン面積から**天井面積**を拾い、仕上表に面積欄を足す。
6. 領域をクリップ範囲にして**目透かし目地**を割り付ける。

`pip install shapely` が要ります。単体 exe 化（PyInstaller）する場合、shapely 込みで 30MB 前後になります。

## 設計のメモ（読み書きの要点）

`jwc_temp.txt` は CP932 / CRLF。**座標は実寸 mm**。1/100 の図面で `7280` とあれば 7.28m。`hs` は縮尺の分母がレイヤグループ 0-15 分だけ並ぶ。

一方 **`hcw`/`hch`/`hcd`（文字の幅・高さ・間隔）は図寸 mm**。ここが混ざるのが最大の落とし穴で、実機で最初に踏んだ。`SYMBOL_DY` などの設定値も図寸 mm なので、座標に使うときは必ず縮尺の分母を掛ける（`CeilingPlan.scale(lg)`）。掛け忘れると 1/100 の図面で長さが 1/100 になり、記号も仕上表も一点に潰れて重なる。`SET SCALE` で固定もできる。

出力側で `bz` を書くと座標を図寸として解釈させられるが、複写する元要素は実寸で受け取っているので使っていない。

```
hcw / hch / hcd   文字種1-10 の 幅 / 高さ / 間隔
hs 100            縮尺 1/100
hp1 x y           REM #1 で指示した点
lg0 ly0 lc2 lt1 cn3   属性。以降の要素に効く
x1 y1 x2 y2       線
ci cx cy r [開始角 終了角 …]   円・円弧
ch x y f3 f4 "文字列           文字（閉じquoteなし）
```

引っかかりやすい点:

- **レイヤ a〜f（16進）**。属性行 `lyf` の属性名を `[a-z]+` で取ると `lyf` 全体が名前になり、レイヤ番号が更新されないまま次の要素へ持ち越される。実機で「レイヤFの壁が丸ごと消える」「建具が別レイヤ扱いで残る」として現れた。属性名は先頭2文字で取る。`src/テスト/sample_16進レイヤ.txt` が回帰用。
- **属性行の見分け**は `lg/ly/lc/lt/cn` のホワイトリストで行う。`[a-z]` 始まりを全部属性扱いにすると、ソリッド `sl` やブロック `bl` が属性として取り込まれ、書き出しにそのまま出てしまう。
- **`ch` の第3・第4フィールド**は「長さ成分」と「終点座標」の2説がある。読み込んだデータから多数決で自動判定している（`Doc.detect_ch_format`）。`SET CHFORMAT rel|abs` で固定可。
- **文字列を書き換えたら長さも取り直す**こと。しないと Jw_cad 側で文字が引き伸ばされる。
- **文字幅の半角判定**は Shift_JIS で1バイトになる文字（ASCII と**半角カナ**）。`ｹｲ酸ｶﾙｼｳﾑ板` のような表記が仕上表に入るので、ここを間違えると欄幅がずれる（移植時に実際に踏んだ）。
- **扱う要素は線・点・円弧・文字だけ**。ソリッドやブロック図形は複写されず、ログに個数が出る。
- ルールファイルは、行頭（桁下げなし）が英大文字だけの語で始まる行だけを項目として読む。それ以外は説明文扱いなので `#` を付けなくてよい。

## 元になった資料

`f07791c0`（1階平面図 S=1/100）と `9137cb8f`（1階天井伏図 S=1/100）の2枚。事務所併用住宅。仕上表の C-1〜C-5 と室名→記号の対応は、この2枚から `src/天伏図ルール.txt` に写してあります。
